# CSE-Batoctoy-Finals
# CRUD REST API with MySQL

A Flask-based REST API that supports full CRUD operations on a `users` table, with output in **JSON or XML**.

1. Clone this repo.
2. Set up MySQL and run the provided SQL script to create `testdb` and `users` table with 20 records.
3. Create a virtual environment:
   ```bash
   python -m venv venv
   source venv/bin/activate  # Linux/macOS
   venv\Scripts\activate     # Windows